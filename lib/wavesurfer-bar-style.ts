/** Shared bar waveform options for WaveSurfer (recording + playback). */
export const waveformBarStyle = {
    waveColor: "hsl(var(--foreground) / 0.5)",
    barWidth: 1,
    barGap: 2,
    barRadius: 1,
    barMinHeight: 10,
    barHeight: 2,
    normalize: true,
} as const
